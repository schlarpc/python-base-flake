{
  "project_name": "",
  "module_name": "{{ cookiecutter.project_name | slugify(separator='_') }}",
  "author_name": "{{ __builtins__.__import__('subprocess').run(['git', 'config', 'user.name'], encoding='utf-8', capture_output=True).stdout.strip() }}",
  "author_email": "{{ __builtins__.__import__('subprocess').run(['git', 'config', 'user.email'], encoding='utf-8', capture_output=True).stdout.strip() }}"
}
