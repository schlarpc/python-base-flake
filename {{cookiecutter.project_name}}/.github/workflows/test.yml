name: Test

on:
  pull_request:
  push:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.0
      - uses: DeterminateSystems/nix-installer-action@v21
      - run: nix build
      - run: nix flake check
